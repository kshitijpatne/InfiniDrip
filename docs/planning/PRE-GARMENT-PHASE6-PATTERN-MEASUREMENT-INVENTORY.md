# Phase 6 pattern-block → measurement-page inventory

_Prepared in Slice 197. The multi-page navigation behavior was approved on
2026-09-22; Slice 198 implements it against this default-options inventory._

This inventory covers the 40 pattern blocks produced by the seven current
garment recipes under their default design options. A block is linked to a
measurement only when changing that declared measurement changes the drafted
piece. The candidate links were checked by perturbing each field around
`STANDARD_M` and a second shifted measurement profile, then comparing the
resulting pieces. This is a code-derived baseline, not a claim that every
non-default option combination has been exhaustively explored.

The measurement control groups are:

- **B — Body measurements:** `chest`, `neck`, `shoulderWidth`, `bicep`,
  `waist`, `hip`, `hipDepth`, `crotchDepth`, `thigh`, `knee` (Measure stage).
- **L — Lengths & shape:** `length`, `armholeDepth`, `sleeveLength`,
  `inseam`, `strapWidth`, `neckDrop`, `neckWidthEase` (Measure stage).
- **F — Fit allowance:** `ease` (a control page assigned to the Fit stage).

These are the current labels generated by `controlsMarkup`. Page indices vary
by garment; a future implementation should target the stable group labels and
field IDs, not hard-coded page numbers. Design-option groups are separate from
these measurement groups.

## Candidate block mappings

| Garment / Pattern block | Related editable measurement fields → control group |
|---|---|
| Tee — FRONT | B: `chest`, `shoulderWidth`; L: `length`, `armholeDepth`; F: `ease` |
| Tee — BACK | B: `chest`, `shoulderWidth`; L: `length`, `armholeDepth`; F: `ease` |
| Tee — SLEEVE | B: `chest`, `shoulderWidth`, `bicep`; L: `armholeDepth`, `sleeveLength`; F: `ease` |
| Darted tee — FITTED FRONT | B: `chest`, `shoulderWidth`; L: `length`, `armholeDepth`; F: `ease` |
| Darted tee — BACK | B: `chest`, `shoulderWidth`; L: `length`, `armholeDepth`; F: `ease` |
| Darted tee — SLEEVE | B: `chest`, `shoulderWidth`, `bicep`; L: `armholeDepth`, `sleeveLength`; F: `ease` |
| Tank — FRONT | B: `chest`; L: `length`, `armholeDepth`, `strapWidth`, `neckDrop`, `neckWidthEase`; F: `ease` |
| Tank — BACK | B: `chest`; L: `length`, `armholeDepth`, `strapWidth`, `neckWidthEase`; F: `ease` |
| Polo — FRONT | B: `chest`, `shoulderWidth`; L: `length`, `armholeDepth`; F: `ease` |
| Polo — BACK | B: `chest`, `shoulderWidth`; L: `length`, `armholeDepth`; F: `ease` |
| Polo — SLEEVE | B: `chest`, `shoulderWidth`, `bicep`; L: `armholeDepth`, `sleeveLength`; F: `ease` |
| Polo — BUTTON PLACKET | **No meaningful measurement link.** Its dimensions respond to `placketLength` and `placketWidth` in the Front closure option group. |
| Polo — BUTTONHOLE PLACKET | **No meaningful measurement link.** Its dimensions respond to `placketLength` and `placketWidth` in the Front closure option group. |
| Polo — OUTER COLLAR STAND | B: `chest` |
| Polo — INNER COLLAR STAND | B: `chest` |
| Polo — UPPER POINTED COLLAR | B: `chest` |
| Polo — UNDER POINTED COLLAR | B: `chest` |
| Woven shirt — WOVEN FRONT | B: `neck`, `chest`, `shoulderWidth`, `waist`, `hip`, `hipDepth`; L: `length`, `armholeDepth`; F: `ease` |
| Woven shirt — WOVEN BACK LOWER | B: `chest`, `shoulderWidth`, `waist`, `hip`, `hipDepth`; L: `length`, `armholeDepth`; F: `ease` |
| Woven shirt — WOVEN BACK YOKE | B: `neck`, `chest`, `shoulderWidth`; L: `armholeDepth`; F: `ease` |
| Woven shirt — WOVEN PATCH POCKET | **No meaningful measurement link.** Its dimensions respond to `pocketWidth` and `pocketHeight` in the Pocket option group. |
| Woven shirt — OUTER WOVEN STAND | B: `neck` |
| Woven shirt — INNER WOVEN STAND | B: `neck` |
| Woven shirt — UPPER POINTED WOVEN COLLAR | B: `neck` |
| Woven shirt — UNDER POINTED WOVEN COLLAR | B: `neck` |
| Woven shirt — WOVEN BUTTON PLACKET | B: `neck`; L: `length` |
| Woven shirt — WOVEN BUTTONHOLE PLACKET | B: `neck`; L: `length` |
| Woven shirt — WOVEN SHORT SLEEVE | B: `chest`, `shoulderWidth`, `bicep`; L: `armholeDepth`, `sleeveLength`; F: `ease` |
| Woven shirt — WOVEN FOLDED SLEEVE BAND | B: `bicep`; F: `ease` |
| Skirt — FRONT | B: `waist`, `hip`, `hipDepth`; L: `length`; F: `ease` |
| Skirt — BACK | B: `waist`, `hip`, `hipDepth`; L: `length`; F: `ease` |
| Skirt — WAISTBAND | B: `waist`; F: `ease` |
| Trouser — TROUSER FRONT LEFT | B: `waist`, `hip`, `hipDepth`, `crotchDepth`, `thigh`, `knee`; L: `inseam`; F: `ease` |
| Trouser — TROUSER FRONT RIGHT | B: `waist`, `hip`, `hipDepth`, `crotchDepth`, `thigh`, `knee`; L: `inseam`; F: `ease` |
| Trouser — TROUSER BACK LEFT | B: `waist`, `hip`, `hipDepth`, `crotchDepth`, `thigh`, `knee`; L: `inseam`; F: `ease` |
| Trouser — TROUSER BACK RIGHT | B: `waist`, `hip`, `hipDepth`, `crotchDepth`, `thigh`, `knee`; L: `inseam`; F: `ease` |
| Trouser — TROUSER WAISTBAND | B: `waist`; F: `ease` |
| Trouser — TROUSER FLY SHIELD | **No meaningful measurement link.** Its dimensions respond to `flyLength` in the Front closure option group. |
| Trouser — TROUSER POCKET BAG LEFT | B: `waist`; F: `ease` |
| Trouser — TROUSER POCKET BAG RIGHT | B: `waist`; F: `ease` |

## Mapping scope and approved multi-page behavior

Of the 40 blocks, 36 have a measurement link and four are option-only. Twenty-
eight of the 36 measurement-linked blocks span multiple groups: 21 link to B,
L, and F; two link to B and L; five link to B and F. Only eight map to one
measurement group. In particular, the `ease` control belongs to the Fit stage,
not the Measure stage. A block click therefore cannot reveal every listed
field by opening one existing page.

**Maintainer decision (approved 2026-09-22):** activating a block opens its
first linked group and offers navigation links to every other linked group,
highlighting that page's related fields. The order is Body measurements →
Lengths & shape → Fit allowance. Keep the existing pages and inputs in place;
do not duplicate or move measurements. Keyboard activation follows the same
path, focuses the first related field on each destination, and changes no
measurement or design value.

For the four option-only blocks, explain which design-option group controls
the piece; do not navigate to or highlight an unrelated measurement. Other
blocks may also respond to design options; those controls are outside this
measurement-specific inventory and are not silently treated as measurements.

This is a code-derived baseline for default design options, not a claim that
every non-default option combination has been exhaustively tested. The
implementation uses the existing field IDs and group labels, resolving page
indices from the live controls.
